pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
}

include ':onlySub'
project(':onlySub').projectDir = file('src/onlySub')